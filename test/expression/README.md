# expression

Design notes intentionally cleared. The previous content was treated as a
misleading source and is not used to derive expected values. Expression
feature test scenarios and their expected results are built directly from the
correct Drizzle / SQL spec inside the test files in this directory.
